# توثيق تطبيق رحلتي (Rihlaty App)

## 📋 جدول المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [البنية المعمارية](#البنية-المعمارية)
3. [الشاشات والميزات](#الشاشات-والميزات)
4. [نظام التصميم](#نظام-التصميم)
5. [التثبيت والإعداد](#التثبيت-والإعداد)
6. [دليل الاستخدام](#دليل-الاستخدام)
7. [الأمان](#الأمان)

## 🎯 نظرة عامة

**رحلتي** هو تطبيق جوال متطور يربط بين التجار وسائقي التكاتك لتسهيل عمليات نقل البضائع في مصر والسودان. التطبيق مبني باستخدام **Flutter** و **Supabase** ويتميز بتصميم عصري وأنيميشنات احترافية.

### الأهداف الرئيسية
- ✅ تسهيل عملية طلب الرحلات للتجار
- ✅ توفير فرص عمل للسائقين
- ✅ ضمان الأمان والموثوقية
- ✅ توفير تجربة مستخدم ممتازة

## 🏗️ البنية المعمارية

### هيكل المشروع

```
lib/
├── config/
│   ├── theme.dart           # نظام الألوان والثيم
│   ├── constants.dart       # الثوابت والإعدادات
│   └── router.dart          # نظام التوجيه
├── models/
│   ├── user_model.dart      # نموذج المستخدم
│   └── trip_model.dart      # نموذج الرحلة
├── providers/
│   └── auth_provider.dart   # مزود المصادقة
├── screens/
│   ├── auth/                # شاشات المصادقة
│   ├── merchant/            # شاشات التاجر
│   ├── driver/              # شاشات السائق
│   ├── admin/               # شاشات الإدارة
│   └── shared/              # الشاشات المشتركة
├── services/
│   └── supabase_service.dart # خدمات Supabase
├── utils/                   # الأدوات المساعدة
├── widgets/                 # المكونات المعاد استخدامها
└── main.dart               # نقطة الدخول الرئيسية
```

### معمارية التطبيق

```
┌─────────────────────────────────────────┐
│         Flutter UI Layer                │
│  (Screens, Widgets, Animations)        │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│      Riverpod State Management          │
│  (Providers, Controllers)               │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│         Services Layer                  │
│  (Supabase, APIs, Local Storage)       │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│      Backend & Database                 │
│  (Supabase, PostgreSQL)                │
└─────────────────────────────────────────┘
```

## 📱 الشاشات والميزات

### شاشات المصادقة (Auth Screens)

#### 1. **Splash Screen** (`splash_screen.dart`)
- شاشة البداية مع أنيميشنات احترافية
- عرض شعار التطبيق والرسالة الترحيبية
- انتقال تلقائي لشاشة تسجيل الدخول

**الأنيميشنات:**
- تكبير وتدوير الشعار
- انزلاق النص
- نقاط تحميل متحركة

#### 2. **Login Screen** (`login_screen.dart`)
- تسجيل دخول بالهاتف وكلمة المرور
- خيار تسجيل الدخول عبر Google و Apple
- رابط نسيان كلمة المرور

**الميزات:**
- حقول إدخال محسّنة
- مؤشر كلمة مرور
- رسائل خطأ واضحة

#### 3. **Register Screen** (`register_screen.dart`)
- تسجيل حساب جديد
- اختيار الدور (تاجر أو سائق)
- الموافقة على الشروط والأحكام

**الميزات:**
- اختيار الدور مع رسوم متحركة
- التحقق من صحة البيانات
- رسائل خطأ تفصيلية

### شاشات التاجر (Merchant Screens)

#### 1. **Merchant Home Screen** (`merchant_home_screen.dart`)
- الشاشة الرئيسية للتاجر
- عرض الرحلات الجارية والمعلقة
- إحصائيات اليوم

**المكونات:**
- بطاقة ترحيب مع معلومات المستخدم
- شبكة الخدمات السريعة (4 خدمات)
- بطاقة الإحصائيات
- قائمة الرحلات الأخيرة

#### 2. **Request Trip Screen** (`request_trip_screen.dart`)
- طلب رحلة جديدة
- اختيار نقاط الالتقاء والتسليم
- اختيار نوع الحزمة

**الميزات:**
- معاينة الخريطة
- حقول الموقع مع أيقونات
- اختيار نوع الحزمة (عام، سريع، فوري)
- حساب السعر التلقائي
- وصف الحزمة (اختياري)

### شاشات السائق (Driver Screens)

#### 1. **Driver Home Screen** (`driver_home_screen.dart`)
- الشاشة الرئيسية للسائق
- حالة الاتصال (متصل/غير متصل)
- الرحلات المتاحة

**المكونات:**
- بطاقة الحالة مع زر التبديل
- إحصائيات الأداء
- الخدمات السريعة
- قائمة الرحلات المتاحة

### الشاشات المشتركة (Shared Screens)

#### 1. **Trip Tracking Screen** (`trip_tracking_screen.dart`)
- تتبع الرحلة في الوقت الفعلي
- معلومات السائق
- تفاصيل الرحلة

**الميزات:**
- معاينة الخريطة
- حالة الرحلة مع مؤشر التقدم
- معلومات السائق وتقييمه
- أزرار الاتصال والرسائل
- تفاصيل الرحلة

#### 2. **Wallet Screen** (`wallet_screen.dart`)
- إدارة المحفظة والأموال
- عرض الرصيد والدخل والإنفاق
- سجل المعاملات

**الميزات:**
- بطاقة الرصيد الرئيسية
- أزرار الإجراءات (شحن، تحويل، سحب)
- تبويبات المعاملات
- قائمة المعاملات التفصيلية

#### 3. **Profile Screen** (`profile_screen.dart`)
- الملف الشخصي للمستخدم
- إعدادات الحساب
- إعدادات التطبيق

**الميزات:**
- رأس الملف الشخصي
- إحصائيات المستخدم
- قائمة الإعدادات
- خيارات اللغة والوضع الليلي
- زر تسجيل الخروج

### شاشات الإدارة (Admin Screens)

#### 1. **Admin Dashboard Screen** (`admin_dashboard_screen.dart`)
- لوحة التحكم الرئيسية
- عرض الإحصائيات الرئيسية
- إدارة النظام

**المكونات:**
- بطاقات KPI (إجمالي الرحلات، الإيرادات، المستخدمين)
- شبكة إدارة النظام (6 خيارات)
- قائمة الأنشطة الأخيرة

## 🎨 نظام التصميم

### نظام الألوان

```dart
// الألوان الأساسية
primary: #B71C1C (أحمر داكن)
secondary: #FFDAD6 (ذهبي فاتح)
success: #10B981 (أخضر)
error: #EF4444 (أحمر)
warning: #F59E0B (برتقالي)

// الألوان المحايدة
textPrimary: #1F2937 (رمادي غامق)
textSecondary: #6B7280 (رمادي متوسط)
textHint: #9CA3AF (رمادي فاتح)
borderColor: #E5E7EB (رمادي جداً فاتح)
```

### التدرجات اللونية

```dart
primaryGradient: LinearGradient(
  colors: [#B71C1C, #DC2626]
)

secondaryGradient: LinearGradient(
  colors: [#FFDAD6, #FCA5A5]
)
```

### الخطوط

- **الخط الأساسي**: Cairo (عربي)
- **الخط الثانوي**: Tajawal (عربي)
- **أحجام الخطوط**:
  - H1: 32px (عناوين رئيسية)
  - H2: 24px (عناوين فرعية)
  - H3: 18px (عناوين صغيرة)
  - Body: 14px (نص عادي)
  - Small: 12px (نص صغير)

### الأنيميشنات

#### 1. **Fade In Animation**
```dart
FadeTransition(
  opacity: animation,
  child: widget,
)
```

#### 2. **Slide Animation**
```dart
SlideTransition(
  position: animation,
  child: widget,
)
```

#### 3. **Scale Animation**
```dart
ScaleTransition(
  scale: animation,
  child: widget,
)
```

#### 4. **Rotation Animation**
```dart
RotationTransition(
  turns: animation,
  child: widget,
)
```

### المسافات والحدود

```dart
// المسافات
paddingSmall: 8px
paddingMedium: 16px
paddingLarge: 24px

// الحدود الدائرية
radiusSmall: 8px
radiusMedium: 12px
radiusLarge: 16px

// ارتفاع الأزرار
buttonHeightSmall: 40px
buttonHeightMedium: 48px
buttonHeightLarge: 56px
```

## 🚀 التثبيت والإعداد

### المتطلبات
- Flutter 3.11.5 أو أحدث
- Dart 3.11.5 أو أحدث
- Android SDK (API 21+)
- iOS SDK (11.0+)

### خطوات التثبيت

```bash
# 1. استنساخ المشروع
git clone <repository-url>
cd rihlaty_app

# 2. تثبيت المكتبات
flutter pub get

# 3. إنشاء ملف الإعدادات
cp .env.example .env

# 4. تحديث بيانات Supabase
# عدّل lib/config/constants.dart بـ:
# - SUPABASE_URL
# - SUPABASE_ANON_KEY

# 5. تشغيل التطبيق
flutter run
```

### إعداد Supabase

1. **إنشاء حساب Supabase**
   - اذهب إلى https://supabase.com
   - أنشئ حساباً جديداً
   - أنشئ مشروعاً جديداً

2. **الحصول على المفاتيح**
   - Project URL
   - Anon Public Key

3. **تحديث الإعدادات**
   ```dart
   // lib/config/constants.dart
   const String SUPABASE_URL = 'your_project_url';
   const String SUPABASE_ANON_KEY = 'your_anon_key';
   ```

## 📖 دليل الاستخدام

### للتاجر

1. **تسجيل الحساب**
   - افتح التطبيق
   - اختر "إنشاء حساب"
   - اختر "تاجر"
   - أدخل بيانات الحساب

2. **طلب رحلة**
   - اضغط "رحلة جديدة"
   - أدخل نقطة الالتقاء
   - أدخل نقطة التسليم
   - اختر نوع الحزمة
   - اضغط "إرسال الطلب"

3. **تتبع الرحلة**
   - انتظر قبول السائق
   - شاهد موقع السائق على الخريطة
   - تواصل مع السائق عند الحاجة

4. **الدفع والتقييم**
   - ادفع عند انتهاء الرحلة
   - قيّم السائق والخدمة

### للسائق

1. **تسجيل الحساب**
   - افتح التطبيق
   - اختر "إنشاء حساب"
   - اختر "سائق"
   - أدخل بيانات الحساب

2. **تفعيل الحساب**
   - قم بتحميل المستندات المطلوبة
   - انتظر التحقق من الإدارة

3. **البحث عن رحلات**
   - فعّل "متصل"
   - اعرض الرحلات المتاحة
   - اقبل الرحلة المناسبة

4. **تنفيذ الرحلة**
   - التقط التاجر من نقطة الالتقاء
   - اتجه إلى نقطة التسليم
   - أكمل الرحلة

5. **سحب الأموال**
   - اذهب إلى المحفظة
   - اضغط "سحب"
   - أدخل المبلغ والحساب البنكي

## 🔐 الأمان

### مصادقة المستخدم
- تسجيل دخول آمن عبر Supabase Auth
- تشفير كلمات المرور
- جلسات آمنة

### حماية البيانات
- تشفير البيانات الحساسة
- قواعد أمان صارمة في قاعدة البيانات
- التحقق من صحة المدخلات

### الخصوصية
- عدم مشاركة البيانات الشخصية
- سياسة خصوصية واضحة
- حق المستخدم في حذف بيانات حسابه

## 🐛 استكشاف الأخطاء

### المشكلة: التطبيق لا يبدأ
**الحل:**
```bash
flutter clean
flutter pub get
flutter run
```

### المشكلة: خطأ في الاتصال بـ Supabase
**الحل:**
- تحقق من المفاتيح في `constants.dart`
- تحقق من الاتصال بالإنترنت
- تحقق من حالة خادم Supabase

### المشكلة: الأنيميشنات بطيئة
**الحل:**
- قلل عدد الأنيميشنات
- استخدم `--release` عند التشغيل
- تحقق من أداء الجهاز

## 📊 الإحصائيات

| المقياس | القيمة |
|--------|--------|
| عدد الشاشات | 10+ |
| عدد المكونات | 50+ |
| عدد الأنيميشنات | 30+ |
| حجم التطبيق | ~50 MB |
| الحد الأدنى للـ Android | API 21 |
| الحد الأدنى للـ iOS | 11.0 |

## 📚 المراجع

- [Flutter Documentation](https://flutter.dev)
- [Supabase Documentation](https://supabase.com/docs)
- [Riverpod Documentation](https://riverpod.dev)
- [Go Router Documentation](https://pub.dev/packages/go_router)

## 📞 الدعم والتواصل

للأسئلة والاستفسارات:
- البريد الإلكتروني: support@rihlaty.com
- الموقع: www.rihlaty.com
- رقم الدعم: +966-XX-XXXX-XXXX

---

**آخر تحديث**: يناير 2024
**الإصدار**: 1.0.0
